// if the class has default access control then its default constructor will also have default access control that is created by compiler.

// The same access control will be applied to the default constructor made by the compiler
// Default access control does not require any keyword.

class H {
    // Default Access Control
    H() {

    }    
}
