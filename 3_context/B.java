class B {
    // instance level variables
    int a;
    String b;

    // class level variables
    static float c;
    static Integer d;

    // local level variables
    void pro(char e, Double f) {
        boolean g;
        Float h;
    }
}