class C {
    
    int a;   // primitive type, instance level variable
    String b;   // class type, instance level variable
    
    static float c;    // primitive type, class level variable
    static Integer d;   // class type, class level variables

    // local level variables
    void pro(char e, Double f) {  // e - primitive type, local level parameter variable. f - class type, local level parameter variable
        boolean g;  // g - primitive type, local level variable 
        Float h;    // h - class type, local level variable
    }
}